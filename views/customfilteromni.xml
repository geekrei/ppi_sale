<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="customfilteromni.list">
      <field name="name">customfilteromni list</field>
      <field name="model">customfilteromni.customfilteromni</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="customfilteromni.action_window">
      <field name="name">customfilteromni window</field>
      <field name="res_model">customfilteromni.customfilteromni</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="customfilteromni.action_server">
      <field name="name">customfilteromni server</field>
      <field name="model_id" ref="model_customfilteromni_customfilteromni"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="customfilteromni" id="customfilteromni.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="customfilteromni.menu_1" parent="customfilteromni.menu_root"/>
    <menuitem name="Menu 2" id="customfilteromni.menu_2" parent="customfilteromni.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="customfilteromni.menu_1_list" parent="customfilteromni.menu_1"
              action="customfilteromni.action_window"/>
    <menuitem name="Server to list" id="customfilteromni" parent="customfilteromni.menu_2"
              action="customfilteromni.action_server"/>
    -->
    <!-- <record id="view_hr_holidays_search" model="ir.ui.view">
      <field name="name">hr.holidays.view.inherit.omni</field>
      <field name="model">hr.holidays</field>
      <field name="inherit_id" ref="hr_holidays.view_hr_holidays_filter"/>
      <field name="arch" type="xml">
        <xpath expr="//search" position="inside">
            <filter name="yearnow" string="Current Year (custom)" domain="[('date_from', 'ilike', current_year)]" help="Displays leaves and allocations for current year only."/>
        </xpath>
      </field>
    </record> -->
  </data>
</odoo>